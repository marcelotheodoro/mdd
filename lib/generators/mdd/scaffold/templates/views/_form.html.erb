<%%= form_for(@<%= @model.singular_name %><%= ', :remote => true, :html => {:class => :mdwa_ajax}' if options.ajax %>) do |f| %>

	<%- unless options.ajax %>
		<%% if @<%= @model.singular_name %>.errors.any? %>
		    <div id="error_explanation">
		      <h2><%%= pluralize(@<%= @model.singular_name %>.errors.count, t('system.error')) %> :</h2>

		      <ul>
		      <%% @<%= @model.singular_name %>.errors.full_messages.each do |msg| %>
		        <li><%%= msg %></li>
		      <%% end %>
		      </ul>
		    </div>
		<%% end %>
	<%- end %>

	<div class="yui3-g">
		<div class="yui3-u">
			<%- @model_attributes.each do |attr| %>
			<div class="field">
	  	    	<%%= f.label :<%= attr.name %> %> 
	  	    	<%= attr.form_field %>
	  	  	</div>
  	  		<%- end %>
  	  	</div>
	</div>

	<div class="actions">
		<%- if !options.ajax %>
		<%%= link_to t('system.cancel_button'), <%= @model.object_name.pluralize %>_path, :class => :cancel %> <%- else %>
		<%%= link_to t('system.cancel_button'), '#', :class => :cancel %> <%- end %>
	    
	    <%%= f.submit :class => :button %>
	</div>

<%% end %>